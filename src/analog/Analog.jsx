import React from 'react';
import analog from '../assets/analog/analog.png'

const Analog = () => {
    return (
        <div>
            <img src={analog} />
        </div>
    );
};

export default Analog;